# Core deep learning stack
# Install PyTorch matching your CUDA (see README). Blackwell GPUs (B200, sm_100)
# need torch>=2.7.0 built against CUDA 12.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0
torchvision>=0.22.0

# PyTorch Lightning 2.x (imported as `lightning`)
lightning==2.4.0
torchmetrics==1.6.0

# SigLIP 2 backbone (Siglip2 classes require transformers >= 4.53)
transformers==4.53.2
safetensors>=0.4.5

# Configuration (Hydra + OmegaConf)
hydra-core==1.3.2
omegaconf==2.3.0

# Logging
wandb==0.18.7
tensorboard==2.18.0

# Data / vision utils
numpy<2.0
pillow>=10.0.0
opencv-python-headless>=4.9.0
pandas>=2.0.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
tqdm>=4.66.0

# Face detection for scripts/preprocess_ffpp.py (MTCNN)
facenet-pytorch>=2.5.3

# Optional face detectors (uncomment the one you use)
# dlib>=19.24.0
# retina-face>=0.0.17
