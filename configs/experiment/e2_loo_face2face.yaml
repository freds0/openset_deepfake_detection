# @package _global_
# E — protocolo leave-one-manipulation-out (open-set / cross-manipulation, Tabelas I-II).
# Source (train+val): as 3 manipulações restantes + real. Target (test): Face2Face + real.
data:
  train_classes: [real, Deepfakes, FaceSwap, NeuralTextures]
  test_classes: [real, Face2Face]
logger:
  wandb:
    name: e2_loo_face2face
    tags: [v0.2, E, e2_loo_face2face]
