# @package _global_
# E — protocolo leave-one-manipulation-out (open-set / cross-manipulation, Tabelas I-II).
# Source (train+val): as 3 manipulações restantes + real. Target (test): Deepfakes + real.
data:
  train_classes: [real, Face2Face, FaceSwap, NeuralTextures]
  test_classes: [real, Deepfakes]
logger:
  wandb:
    name: e1_loo_deepfakes
    tags: [v0.2, E, e1_loo_deepfakes]
