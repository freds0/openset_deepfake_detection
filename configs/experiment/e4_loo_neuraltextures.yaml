# @package _global_
# E — protocolo leave-one-manipulation-out (open-set / cross-manipulation, Tabelas I-II).
# Source (train+val): as 3 manipulações restantes + real. Target (test): NeuralTextures + real.
data:
  train_classes: [real, Deepfakes, Face2Face, FaceSwap]
  test_classes: [real, NeuralTextures]
logger:
  wandb:
    name: e4_loo_neuraltextures
    tags: [v0.2, E, e4_loo_neuraltextures]
